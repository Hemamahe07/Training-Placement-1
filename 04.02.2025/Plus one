#include <stdio.h>

int main() {
    int num;
    printf("Enter an integer: ");
    scanf("%d", &num);
    
    num += 1; // Adding 1 to the number
    
    printf("Result after adding 1: %d\n", num);
    return 0;
}
